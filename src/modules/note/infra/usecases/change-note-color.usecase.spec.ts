import { ChangeNoteColorUsecase } from './change-note-color.usecase';

describe('ChangeNoteColorUsecase', () => {
  it('should be defined', () => {
    expect(new ChangeNoteColorUsecase()).toBeDefined();
  });
});
